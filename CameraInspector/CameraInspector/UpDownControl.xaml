<UserControl x:Class="CameraInspector.UpDownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CameraInspector"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="PART_TextBox" BorderThickness="0" VerticalAlignment="Center"/>
        <DockPanel Grid.Column="1">            
            <RepeatButton x:Name="PART_ButtonUp"  VerticalAlignment="Center" DockPanel.Dock="Top" Content="▲" Padding="2,0" Margin="0,-1,-1,-0.5"  BorderBrush="{Binding BorderBrush}" FontSize="12" />
            <RepeatButton x:Name="PART_ButtonDown" VerticalAlignment="Center" DockPanel.Dock="Bottom" Content="▼" Padding="2,0" Margin="0,-0.5,-1,-1" BorderBrush="{Binding BorderBrush}" FontSize="12" />           
        </DockPanel>
    </Grid>
</UserControl>
